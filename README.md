<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

# Nutragen AI - Генератор рекламных креативов для Nutra

Приложение для генерации рекламных изображений с использованием AI (Gemini).

## 🚀 Быстрый старт

### Локальный запуск

**Требования:** Node.js 20+

1. Установите зависимости:
   ```bash
   npm install
   ```

2. Создайте файл `.env.local` и добавьте ваш Gemini API ключ:
   ```
   GEMINI_API_KEY=ваш_ключ_здесь
   ```

3. Запустите приложение:
   ```bash
   npm run dev
   ```

4. Откройте браузер по адресу: `http://localhost:3000`

## 📦 Деплой на GitHub Pages

Приложение автоматически деплоится на GitHub Pages при каждом push в ветку `main` или `master`.

### Первоначальная настройка

1. **Создайте репозиторий на GitHub** (если еще не создан)

2. **Загрузите код на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ваш-username/nutragen-ai.git
   git push -u origin main
   ```

3. **Включите GitHub Pages в настройках репозитория:**
   - Перейдите в `Settings` → `Pages`
   - В разделе `Source` выберите `GitHub Actions`
   - Сохраните изменения

4. **Дождитесь завершения деплоя:**
   - Перейдите во вкладку `Actions` в вашем репозитории
   - Дождитесь завершения workflow "Deploy to GitHub Pages"
   - После успешного деплоя ваше приложение будет доступно по адресу:
     `https://ваш-username.github.io/nutragen-ai/`

### Использование API ключа в продакшене

⚠️ **Важно:** Для работы приложения на GitHub Pages пользователям нужно будет ввести свой Gemini API ключ в настройках приложения (через интерфейс). API ключ хранится только в localStorage браузера и не отправляется на сервер.

### Автоматический деплой

После первоначальной настройки каждый push в ветку `main` будет автоматически запускать процесс деплоя. Просто делайте:
```bash
git add .
git commit -m "Ваши изменения"
git push
```

## 🔧 Настройка

### Изменение base path для GitHub Pages

Если вы хотите изменить путь, по которому доступно приложение, отредактируйте файл `.github/workflows/deploy.yml` и измените значение `VITE_BASE_PATH`:

```yaml
VITE_BASE_PATH: /ваш-путь/
```

Для деплоя в корень домена (например, если у вас есть кастомный домен), установите:
```yaml
VITE_BASE_PATH: /
```

## 📝 Примечания

- Приложение использует Gemini API для генерации изображений
- API ключ можно ввести в настройках приложения (хранится в localStorage)
- Для работы требуется активный интернет-соединение
