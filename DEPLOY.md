# 🚀 Инструкция по деплою на GitHub Pages

## Шаги для публикации приложения:

### 1. Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите "New repository"
3. Назовите репозиторий (например, `nutragen-ai`)
4. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
5. Нажмите "Create repository"

### 2. Загрузите код на GitHub

Выполните в терминале в папке проекта:

```bash
# Если еще не инициализирован git
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit"

# Переименуйте ветку в main (если нужно)
git branch -M main

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш username)
git remote add origin https://github.com/YOUR_USERNAME/nutragen-ai.git

# Загрузите код
git push -u origin main
```

### 3. Включите GitHub Pages

1. Зайдите в ваш репозиторий на GitHub
2. Перейдите в **Settings** (вверху справа)
3. В левом меню найдите **Pages**
4. В разделе **Source** выберите **GitHub Actions**
5. Сохраните изменения

### 4. Дождитесь деплоя

1. Перейдите во вкладку **Actions** в вашем репозитории
2. Вы увидите workflow "Deploy to GitHub Pages"
3. Дождитесь завершения (зеленая галочка)
4. После успешного деплоя ваше приложение будет доступно по адресу:
   ```
   https://YOUR_USERNAME.github.io/nutragen-ai/
   ```

### 5. Поделитесь ссылкой!

Теперь вы можете отправить ссылку друзьям, и они смогут использовать ваше приложение прямо в браузере!

## ⚠️ Важные замечания:

- **API ключ**: Пользователям нужно будет ввести свой Gemini API ключ в настройках приложения (через интерфейс)
- **Автоматический деплой**: Каждый раз, когда вы делаете `git push`, приложение автоматически обновляется на GitHub Pages
- **Имя репозитория**: Если вы назвали репозиторий по-другому, измените `VITE_BASE_PATH` в файле `.github/workflows/deploy.yml`

## 🔄 Обновление приложения

Чтобы обновить приложение на GitHub Pages:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Через несколько минут изменения появятся на сайте!

